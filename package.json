{
	"name": "mascot-web",
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"postinstall": "cd lib/CubismWebFramework && npm ci && npm run build || echo done.",
		"dev": "vite",
		"tsc": "vue-tsc --noEmit",
		"build": "vue-tsc && vite build",
		"serve": "vite preview"
	},
	"dependencies": {
		"vue": "^3.4.27"
	},
	"devDependencies": {
		"@types/node": "^20.12.10",
		"@vitejs/plugin-vue": "^5.0.4",
		"typescript": "^5.4.5",
		"vite": "^5.2.11",
		"vue-tsc": "^2.0.16"
	}
}
